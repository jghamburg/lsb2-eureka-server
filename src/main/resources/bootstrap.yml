# tag::secured-config-server[]
spring:
  application:
    name: eureka
  cloud:
    config:
      label: session
      password: password
      uri: http://lsb2-config-server:8888
# end::secured-config-server[]
