# tag::secured-config-server[]
spring:
  application:
    name: lsb2-eureka
  cloud:
    config:
      discovery:
        enabled: true
      label: session
      password: password
      uri: http://localhost:8888
# end::secured-config-server[]
