# tag::secured-config-server[]
spring:
  application:
    name: lsb2-eureka
  cloud:
    config:
      label: session
      password: password
      uri: http://lsb2-config-server:8080
# end::secured-config-server[]
