# tag::secured-config-server[]
spring:
  application:
    name: eureka
  cloud:
    config:
      label: session
      password: password
      uri: ${CONFIG_SERVER_URI:http://localhost:8888}
# end::secured-config-server[]
