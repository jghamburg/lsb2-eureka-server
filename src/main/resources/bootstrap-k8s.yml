# tag::secured-config-server[]
spring:
  application:
    name: lsb2-eureka
  cloud:
    config:
      label: session
      password: password
      uri: ${CONFIG_SERVER_URI:http://lsb2-config-server}
# end::secured-config-server[]
